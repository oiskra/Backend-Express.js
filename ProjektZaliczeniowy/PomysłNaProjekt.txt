kolekcja samochodów
użytkownicy 
kupno samochodów?
sprzedaz?
wyścigi miedzy graczami zarabianie pieniędzy na kupno
rynek ze stałymi samochodami
{
   tier samochodów (nagroda zależna od tieru)
   statysetyki w punktach
}

klasy:
- samochod {
	marka string
	model string
	statystyki Statystyki class
	cena : number
	tier (typescript new type) 
}
-statystyki {
	speed number inrange
	acc number inrange
	grip number inrange
	braking	number inrange
}
-uzytkownik {
	username string
	login string
	pass sting 
	kolekcja samochodów Car[]
	wyścigi: Race[]
	pieniądze number
	(nowy token po każdej zmianie)
}
-wyścig {
	playerOne: User
	carOne: Car
	playerTwo: User
	carTwo: Car
	winner: User
	raceTier: Tier	
} 

architektura aplikacji:
1. user 
	get /user
	get /user/:username
	get /user/cars
	get /user/races
	put /user
	delete /user/:id (ADMIN)
	get /user/all (ADMIN)
	get /market
	post /market/:id (middleware checks if user owns the car)
	delete /market/:id (middleware checks if market has a car)
	post /market (ADMIN)
	put /market/:id (ADMIN) 
	post /race/:username
	get /race/:id
	get race/all (ADMIN)	
3. all
	post /register
	post /login
	post /logout
